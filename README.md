# 爆文分析-写作-反馈优化自进化系统

## 系统简介

这是一个基于 Claude Code Agent 框架构建的多智能体内容创作系统，支持微信公众号、小红书、抖音/视频号三大平台。

### 核心特性

- ✅ **自动迭代优化**：通过 ralph-loop 实现反馈循环
- ✅ **完整任务追踪**：通过 planning-with-files 记录进度
- ✅ **持续学习**：自动提炼和积累爆文规则
- ✅ **智能规则管理**：自动去重、合并、权重调整、淘汰过时规则
- ✅ **即时应用**：新规则自动应用到后续创作
- ✅ **评论分析**：从用户反馈中挖掘真实需求
- ✅ **多平台支持**：微信、小红书、抖音

### 自进化能力

**系统会自动**：
1. 从爆文中发现新规则 → 自动更新规则库
2. 检测相似规则 → 自动合并（避免重复）
3. 根据验证次数 → 自动调整权重
4. 识别低效规则 → 自动归档
5. Writer 读取最新规则 → 自动应用到创作

**完全自动化，无需手动干预！** 详见 [SELF_EVOLUTION.md](SELF_EVOLUTION.md)

## 系统架构

### Skill + Subagent 混合架构

本系统采用创新的混合架构：

1. **Skill 作为指令模板**
   - `analyzer/instructions.md` - 爆文分析师的提示词
   - `writer/instructions.md` - 内容创作者的提示词
   - `reviewer/instructions.md` - 内容审核者的提示词

2. **Subagent 作为执行环境**
   - 使用 Task 工具调用 `general-purpose` subagent
   - 将 skill 的 instructions 作为提示词传递
   - 每个 subagent 有独立的上下文

3. **Orchestrator 负责协调**
   - `viral-content` 主 skill 读取各个 skill 的 instructions
   - 准备上下文数据（规则库、平台配置）
   - 调用 subagent 执行任务
   - 汇总结果并做决策

### 工作流程示意

```
用户调用 /viral-content
    ↓
Orchestrator 读取 writer/instructions.md
    ↓
构建完整提示词 = instructions + 上下文
    ↓
Task(subagent_type="general-purpose", prompt=完整提示词)
    ↓
Writer Subagent 执行任务
    ↓
返回结果给 Orchestrator
    ↓
Orchestrator 继续协调其他 subagent
```

## 快速开始

### 前置要求

1. 已安装 Claude Code (>= 2.0)
2. 已安装 ralph-loop 插件
3. 已安装 planning-with-files skill

### 验证安装

```bash
# 验证 ralph-loop
cat ~/.claude/installed_plugins.json | grep ralph-loop

# 验证 planning-with-files
ls ~/.claude/skills/planning-with-files
```

## 使用方法

### 基础用法：创作内容

**viral-content 内置了自动迭代优化**，会自动循环直到评分达标（≥ 8.0 分）。

#### 示例 1：基于选题创作

```bash
/viral-content create \
  --platform xiaohongshu \
  --topic "如何提高工作效率"
```

**系统会自动**：
1. ✅ 调用 Writer 生成 3 个版本
2. ✅ 调用 Reviewer 评分
3. ✅ 如果评分 < 8.0，根据建议优化
4. ✅ 循环直到评分达标
5. ✅ 保存最终内容到 `workspace/drafts/`

---

#### 示例 2：基于素材创作（新功能）

**场景**：您有一个新闻事件或素材，想基于它创作内容。

**步骤 1：准备素材文件**

创建 `workspace/materials/推迟到校时间.md`：

```markdown
# 推迟到校时间的争议

## 核心事件
- 庆一小学推迟低年级到校时间到9点，第一节课9:45开始
- 实行一年多，老师反馈效果不错

## 评论区高赞观点
1. 家长8点半上班，怎么送孩子（145赞）
2. 取消延时服务，学生苦教师苦（53赞）
3. 早起养成习惯问题（49赞）
...
```

**步骤 2：调用系统**

```bash
/viral-content create \
  --platform xiaohongshu \
  --topic "推迟到校时间的争议" \
  --materials "workspace/materials/推迟到校时间.md"
```

**系统会**：
- ✅ 自动读取素材文件
- ✅ 基于素材生成 3 个版本的内容
- ✅ 自动评分和优化
- ✅ 充分利用素材中的信息点

**也可以直接传递文本**：

```bash
/viral-content create \
  --platform xiaohongshu \
  --topic "推迟到校时间的争议" \
  --materials "庆一小学推迟低年级到校时间到9点。评论区观点：家长上班冲突、早起习惯养成等。"
```

---

#### 示例 3：分析爆文并提炼规则

```bash
/viral-content analyze \
  --platform xiaohongshu \
  --content "🔥7天瘦10斤！这个方法太绝了

姐妹们！我用这个方法7天真的瘦了10斤！

之前试过无数种减肥方法都失败了，节食反弹、运动坚持不下来...直到我发现了这个超简单的方法！

✨ 核心原理
不是节食！不是疯狂运动！而是调整饮食时间和顺序！

💪 我的真实变化
第1天：体重没变，但感觉身体轻松了
第3天：瘦了3斤！肚子明显小了
第7天：瘦了10斤！穿上了S码的裙子！

..."
```

**系统会**：
- ✅ 分析标题、开头、结构、节奏等
- ✅ 提炼出可复用的规则
- ✅ 自动更新规则库（去重、合并、调整权重）
- ✅ 新规则自动应用到后续创作

---

#### 示例 4：查看规则库

```bash
/viral-content show-rules \
  --platform xiaohongshu
```

**输出示例**：
```
小红书规则库（按权重排序）

高权重规则（score > 0.8）：
1. [0.85] 痛点共鸣+解决方案预告
2. [0.83] 数字+动词+结果
3. [0.81] emoji+惊叹+痛点

中等权重规则（0.5 < score ≤ 0.8）：
4. [0.70] 社群召唤式结尾
5. [0.70] 极致化表达
...
```

---

### 高级用法

#### 多平台创作

```bash
# 微信公众号（长文深度）
/viral-content create \
  --platform wechat \
  --topic "职场新人如何快速适应" \
  --materials "workspace/materials/职场适应素材.md"

# 抖音/视频号（短视频脚本）
/viral-content create \
  --platform douyin \
  --topic "3分钟学会时间管理" \
  --materials "workspace/materials/时间管理素材.md"
```

---

#### 结合评论分析

**场景**：您有一篇爆文和它的评论，想从中挖掘用户需求。

```bash
/viral-content analyze \
  --platform xiaohongshu \
  --content "爆文内容...

评论区：
- 用户A：太真实了！我也是这样
- 用户B：有没有适合新手的方法？
- 用户C：时间管理也很重要
..."
```

**系统会额外分析**：
- ✅ 用户痛点（加班严重、没时间运动）
- ✅ 用户需求（新手友好的方法）
- ✅ 情感共鸣点（真实感）
- ✅ 潜在选题（时间管理）
- ✅ 内容优化建议（增加科学依据）

---

### 完整工作流程示例

**场景**：从零开始创作一篇小红书爆文

**步骤 1：准备素材**
```bash
# 创建素材文件
echo "标题：如何在职场中快速成长
素材：
- 新人常见困惑：不知道如何融入团队
- 评论区高频问题：如何与领导沟通
..." > workspace/materials/职场成长.md
```

**步骤 2：创作内容**
```bash
/viral-content create \
  --platform xiaohongshu \
  --topic "职场新人快速成长指南" \
  --materials "workspace/materials/职场成长.md"
```

**步骤 3：查看结果**
```bash
# 系统会自动保存到
# workspace/drafts/xiaohongshu_职场新人快速成长指南_20260113.md

# 查看生成的内容
cat workspace/drafts/xiaohongshu_职场新人快速成长指南_*.md
```

**步骤 4：如果有爆文，分析并提炼规则**
```bash
/viral-content analyze \
  --platform xiaohongshu \
  --content "$(cat 爆文内容.txt)"
```

**步骤 5：规则自动应用到下次创作**
```bash
# 下次创作时，系统会自动读取更新后的规则库
/viral-content create \
  --platform xiaohongshu \
  --topic "另一个选题"
```

---

### 常见问题

**Q: 评分一直达不到 8.0 怎么办？**

A: 系统会自动优化，但如果多次迭代仍不达标：
1. 检查素材是否充足
2. 查看 `progress.md` 了解优化历史
3. 手动调整选题或素材

**Q: 如何查看迭代过程？**

A: 查看 `progress.md` 文件：
```bash
cat progress.md
```

**Q: 生成的内容在哪里？**

A: 在 `workspace/drafts/` 目录：
```bash
ls -lt workspace/drafts/
```

**Q: 如何自定义规则？**

A: 直接编辑规则库文件：
```bash
# 编辑小红书规则
vim knowledge/rules/xiaohongshu/base_rules.json
```

**Q: materials 参数支持什么格式？**

A: 支持两种格式：
1. 文件路径：`workspace/materials/xxx.md`
2. 直接文本：`"标题：xxx 素材：xxx"`

---

### 方式 2: 使用 ralph-loop（可选）

如果您想要更强的控制和监控，可以用 ralph-loop 包裹：

```bash
/ralph-loop "/viral-content create --platform xiaohongshu --topic '如何提高工作效率' --materials 'workspace/materials/效率素材.md'" \
--max-iterations 30 --completion-promise "COMPLETE"
```

**注意**：viral-content 本身已经有迭代优化逻辑，通常不需要外层的 ralph-loop。

---

### 方式 3: 直接使用 Task 工具（测试/调试）

适合测试单个 subagent：

---

## 可移植性说明

### ✅ 完全可移植

这个工作流可以在其他电脑上完全复用！所有组件都已打包在项目中。

**包含的内容**：
1. **Skills 目录** - 所有功能模块
   - `skills/viral-content/` - 主控制器
   - `skills/ralph-loop/` - 自动循环插件
   - `skills/planning-with-files/` - 任务规划插件

2. **知识库和配置**
   - `knowledge/rules/` - 平台规则库（可持续积累）
   - `config/platforms.json` - 平台配置

3. **工作空间**
   - `workspace/drafts/` - 历史生成的内容
   - `workspace/materials/` - 创作素材

### 📦 迁移到新电脑

**步骤 1：克隆仓库**
```bash
git clone <your-repo-url>
cd 写文章吧你
```

**步骤 2：启动 Claude Code**
```bash
claude-code
# 或者
q chat
```

**步骤 3：授权 Skills（首次使用）**

当 Claude Code 提示时，允许以下权限：
- `Skill(viral-content)`
- `Skill(ralph-loop:ralph-loop)`
- `Skill(planning-with-files)`

或者手动创建 `.claude/settings.local.json`：
```json
{
  "permissions": {
    "allow": [
      "Skill(viral-content)",
      "Skill(ralph-loop:ralph-loop)",
      "Skill(planning-with-files)"
    ]
  }
}
```

**步骤 4：开始使用**
```bash
# 直接使用，无需额外配置
/viral-content create --platform xiaohongshu --topic "测试选题"
```

### 🔧 环境要求

**必需**：
- Claude Code CLI (>= 2.0)
- Git
- Bash shell

**无需额外安装**：
- 所有脚本使用标准 Bash 命令
- `jq` - Claude Code 内置
- `sed`, `awk`, `grep` - 系统自带

### ⚠️ 运行时文件（自动创建）

以下文件在运行时自动创建，不需要复制：
- `.claude/ralph-loop.local.md` - Ralph-loop 状态
- `task_plan.md` - 任务计划
- `findings.md` - 研究发现
- `progress.md` - 进度日志

### 🎯 关键设计决策

1. **Skills 自包含**: 所有逻辑都在 `skills/` 目录，无外部依赖
2. **相对路径**: 脚本使用 `${CLAUDE_PLUGIN_ROOT}` 环境变量，自动适配
3. **知识库持久化**: 规则库可以跨电脑共享和积累
4. **零配置启动**: 克隆后即可使用

### 📊 目录结构

```
写文章吧你/
├── skills/                          # Skills（可移植）
│   ├── viral-content/              # 主控制器
│   ├── ralph-loop/                # 自动循环
│   └── planning-with-files/       # 任务规划
├── knowledge/                      # 知识库（可移植）
│   └── rules/                     # 规则库
│       ├── wechat/
│       ├── xiaohongshu/
│       └── douyin/
├── config/                         # 配置（可移植）
│   └── platforms.json
├── workspace/                      # 工作空间（可移植）
│   ├── drafts/                    # 生成的内容
│   └── materials/                 # 创作素材
├── .claude/                        # 运行时配置（自动创建）
│   └── settings.local.json        # 权限配置
├── task_plan.md                   # 运行时创建
├── findings.md                    # 运行时创建
├── progress.md                    # 运行时创建
└── README.md
```

### 🚀 团队协作

**场景 1：共享规则库**
```bash
# 成员 A 分析爆文，更新规则库
/viral-content analyze --platform xiaohongshu --content "..."

# 提交到 git
git add knowledge/rules/
git commit -m "更新小红书规则库"
git push

# 成员 B 拉取最新规则
git pull

# 成员 B 的创作会自动使用最新规则
/viral-content create --platform xiaohongshu --topic "..."
```

**场景 2：共享素材库**
```bash
# 成员 A 准备素材
echo "素材内容..." > workspace/materials/新素材.md
git add workspace/materials/
git commit -m "添加新素材"
git push

# 成员 B 使用素材
git pull
/viral-content create --materials "workspace/materials/新素材.md"
```

### 💡 最佳实践

1. **定期提交规则库**: 每次分析爆文后提交更新
2. **共享优质素材**: 将有价值的素材提交到 `workspace/materials/`
3. **保留历史草稿**: `workspace/drafts/` 可以作为参考
4. **版本控制**: 使用 git 分支管理不同的实验

---
